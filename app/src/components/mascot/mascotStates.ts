/**
 * Mascot expression states. The character itself is hand-drawn SVG
 * (see Mascot.tsx) — each state changes face + posture, not just a frame.
 */
export type MascotState = 'idle' | 'celebrate' | 'sad' | 'thinking';
