import { ChaptersScreen } from '@/features/map/ChaptersScreen';

/** formSheet route: the active subject's chapters as a syllabus (§4a). */
export default function ChaptersRoute() {
  return <ChaptersScreen />;
}
