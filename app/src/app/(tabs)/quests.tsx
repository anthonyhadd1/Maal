import { QuestsScreen } from '@/features/quests/QuestsScreen';

/** TAB 3 « Quêtes » — daily goal + quests + achievements entry. */
export default function QuestsRoute() {
  return <QuestsScreen />;
}
