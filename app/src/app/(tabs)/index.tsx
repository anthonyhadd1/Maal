import { LevelsMap } from '@/features/map/LevelsMap';

/** TAB 1 « Apprendre » — the levels map (hero screen, design_mobile.md §4a). */
export default function LearnRoute() {
  return <LevelsMap />;
}
