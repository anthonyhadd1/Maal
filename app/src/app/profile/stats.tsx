import { StatsScreen } from '@/features/profile/StatsScreen';

/** PUSH /profile/stats — detailed stats (premium-gated sections). */
export default function ProfileStatsRoute() {
  return <StatsScreen />;
}
