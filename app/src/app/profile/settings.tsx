import { SettingsScreen } from '@/features/profile/SettingsScreen';

/** PUSH /profile/settings — account, preferences, league opt-in, danger zone. */
export default function ProfileSettingsRoute() {
  return <SettingsScreen />;
}
