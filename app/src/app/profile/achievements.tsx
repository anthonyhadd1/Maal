import { AchievementsScreen } from '@/features/profile/AchievementsScreen';

/** PUSH /profile/achievements — trophies grid. */
export default function ProfileAchievementsRoute() {
  return <AchievementsScreen />;
}
