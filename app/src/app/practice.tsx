import { PracticeScreen } from '@/features/practice/PracticeScreen';

/** PUSH /practice — révision queue preview + entry into /session/practice. */
export default function PracticeRoute() {
  return <PracticeScreen />;
}
