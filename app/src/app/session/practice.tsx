import { SessionScreen } from '@/features/session/SessionScreen';

/** MODAL /session/practice — spaced-repetition mistake review quiz. */
export default function SessionPracticeRoute() {
  return <SessionScreen practice />;
}
