import { PaywallScreen } from '@/features/paywall/PaywallScreen';

/**
 * MODAL /paywall — premium upsell / status; opened from locks everywhere
 * (premium nodes, hearts modal, 402 responses, profile upsell).
 */
export default function PaywallRoute() {
  return <PaywallScreen />;
}
