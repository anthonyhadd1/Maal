import { TrackScreen } from '@/features/onboarding/TrackScreen';

/** Onboarding 1/4 — « Que prépares-tu ? » (track: concours vs. spécialité). */
export default function OnboardingTrackRoute() {
  return <TrackScreen />;
}
